int main()
{
	int a,b,x;
	int gcd(int,int);//函数原型的声明
	printf("enter a,b:");
	scanf("%d,%d",&a,&b);
	x = gcd(a,b);//函数的调用
	printf("%d\n",x);

   return 0;
}

int gcd(int u,int v)
{
   if(v==0)  return u;
   else      return gcd(v,u%v);

}
